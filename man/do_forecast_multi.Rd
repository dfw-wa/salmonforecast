% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_forecast_multi.R
\name{do_forecast_multi}
\alias{do_forecast_multi}
\title{Run do_forecast() across multiple populations and/or ages}
\usage{
do_forecast_multi(
  dat,
  group_vars = intersect(c("population", "age"), names(dat)),
  forecast_ages = NULL,
  total_ages = forecast_ages,
  add_lag_age_covariate = TRUE,
  lag_age_covariate_name = NULL,
  log_lag_age_covariate = TRUE,
  standardize_lag_age_covariate = TRUE,
  aggregate_by = group_vars,
  total_model = "best",
  parallel_groups = FALSE,
  n_cores_groups = 1,
  ...
)
}
\arguments{
\item{dat}{A data frame as required by \code{do_forecast()}, optionally with additional
\code{population} and/or \code{age} columns identifying the group each row belongs to. \code{age} must
be numeric (e.g., 3, 4, 5) so that the next-younger age class can be identified for the
lagged-covariate feature.}

\item{group_vars}{Character vector of grouping columns to split on. Defaults to whichever
of \code{c("population", "age")} are present in \code{dat}. If neither is present, \code{dat} is run
as a single group (equivalent to calling \code{do_forecast()} directly). If \code{dat} has a
\code{population} and/or \code{age} column with more than one distinct value that is \emph{not}
included in \code{group_vars}, it is added automatically (with a warning) -- rows from
distinct populations/ages are never silently combined into a single modeled series.
Use \code{aggregate_by} to control which of these dimensions get summed together afterward
for the retrospective performance evaluation.}

\item{forecast_ages}{Optional vector of age values to actually forecast (others are
dropped entirely before modeling). Defaults to all ages present in \code{dat}. Use this to,
e.g., forecast a youngest age class purely so it is available as a lagged covariate,
without necessarily including it in the summed total (see \code{total_ages}).}

\item{total_ages}{Optional vector of age values to include when computing the
summed-across-ages retrospective performance total. Defaults to \code{forecast_ages}. Must
be a subset of \code{forecast_ages}. Use this to exclude a youngest age class from the total
while still forecasting it (e.g., to use it as a lagged covariate for older ages).}

\item{add_lag_age_covariate}{Logical; if \code{TRUE} (default) and both \code{population} and
\code{age} columns are present (or a single, implicit population), a lagged younger-age
covariate is precomputed via \code{add_lag_age_covariate()} and automatically appended to
the \code{covariates} argument passed to \code{do_forecast()} for any age group that has a
younger age class available in the data.}

\item{lag_age_covariate_name}{Column name to use for the lagged younger-age covariate.
If \code{NULL} (default), a name is chosen automatically based on
\code{log_lag_age_covariate} (\code{"lag1_log_age_minus1"} if \code{TRUE}, otherwise
\code{"lag1_age_minus1"}), matching \code{add_lag_age_covariate()}'s own default naming.}

\item{log_lag_age_covariate}{Logical; if \code{TRUE} (default), the lagged younger-age
covariate is log-transformed before use, matching how the abundance response itself is
modeled on the log scale (via \code{auto.arima(lambda = 0, ...)}) and how other
abundance-derived covariates (e.g. \code{lag1_log_Jack}) are log-transformed in
\code{make_dat()}.}

\item{standardize_lag_age_covariate}{Logical; if \code{TRUE} (default), the (optionally
logged) lagged covariate is standardized to mean 0 / sd 1 within each \verb{(population, age)} group, matching the scaling \code{make_dat()} applies to its other covariates.}

\item{aggregate_by}{Character vector, a subset of \code{group_vars}, specifying which
dimension(s) to sum across for the retrospective performance evaluation (e.g., \code{"age"}
to evaluate a summed total run per population, \code{"population"} to evaluate a summed
total across populations per age, or both for a grand total). Set to \code{character(0)} to
skip aggregation. Defaults to all of \code{group_vars}.}

\item{total_model}{Which per-group ensemble model to use when building the summed total
series; passed through to \code{aggregate_group_performance()}. Defaults to \code{"best"}
(auto-select the top-performing ensemble per group); alternatively supply a fixed
ensemble name (e.g., \code{"MAPE_weighted"}), a vector of several such names, or \code{"all"} to
compute the aggregated total/performance for \code{"best"} plus every ensemble-weighting
method common to all groups, so you can compare summed-total retrospective performance
across weighting schemes (see \code{aggregate_group_performance()} for the resulting output
shape when more than one \code{total_model} is used).}

\item{parallel_groups}{Logical; whether to run the per-group \code{do_forecast()} calls in
parallel (across groups) rather than sequentially. If \code{TRUE}, consider setting the
\code{n_cores} argument passed through \code{...} to \code{1} to avoid nested parallelism.}

\item{n_cores_groups}{Number of parallel workers to use across groups when
\code{parallel_groups = TRUE}.}

\item{...}{Additional arguments passed through to \code{do_forecast()} for every group (e.g.,
\code{covariates}, \code{TY_ensemble}, \code{slide}, \code{k}, \code{min_vars}, \code{max_vars}, \code{n_cores}, etc.).}
}
\value{
A list with elements:
\itemize{
\item \code{by_group}: a named list of the full \code{do_forecast()} output for each group.
\item \code{group_keys}: a data frame giving the grouping-variable values for each element of
\code{by_group} (same row order).
\item \code{combined_forecasts}: a single tidy data frame of per-year ensemble forecasts across
all groups, tagged with the grouping columns, for easy \code{dplyr} filtering/plotting.
\item \code{combined_performance}: a single tidy data frame of each group's own (un-aggregated)
retrospective forecast skill, tagged with the grouping columns.
\item \code{aggregated_performance}: the output of \code{aggregate_group_performance()} (\code{NULL} if
\code{aggregate_by} is empty), giving the summed-across-group retrospective performance.
}
}
\description{
A wrapper around \code{do_forecast()} that splits an input dataset by
\code{population} and/or \code{age} column(s) and runs the full covariate-selection /
one-step-ahead / rolling-performance / ensembling pipeline independently for each
group. Optionally adds a lagged younger-age-class covariate (the observed abundance of
age - 1 in the previous year, useful for forecasting older age classes), restricts
which age classes are actually forecast, and computes a summed-across-group
retrospective performance evaluation (e.g., total run size summed across ages, or a
combined total summed across populations).
}
